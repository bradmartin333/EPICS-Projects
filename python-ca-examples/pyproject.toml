[project]
name = "python-ca-examples"
version = "0.1.0"
description = "Examples for using python-ca with EPICS Channel Access"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyepics>=3.5.8",
]
